# Nesting

`<div>` is like a container. You can use Javascript and CSS to redner or give it a function to a `<div>`

The picture below is the structure that a HTML will be like.
<img src="HTML_document_tree.png">
